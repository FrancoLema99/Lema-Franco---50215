from django.apps import AppConfig


class AplicacionfinalConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'aplicacionFinal'
    verbose_name = "Panel de World's Copy"
